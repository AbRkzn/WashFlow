# WashFlow — Project Brief

Offline-first car wash management system. React Native + Expo + TypeScript + Expo Router + NativeWind, Zustand + TanStack Query, SQLite + Drizzle ORM, Supabase, Expo Notifications (FCM deferred). Portfolio-first, designed to be deployable.

## 1. Product Summary

- Single-branch car wash management system for check-in, queueing, washing, and payment.
- Supports BOTH appointments (fixed 30-min slots, configurable) and walk-ins.
- Fully offline-capable: check-in, queue, payments, job completion, appointments, inventory, expenses all work without internet for 2–7 days.
- Sync engine is the portfolio centerpiece and the single most important feature.
- Cash-only payments in v1. GCash, Maya, and cards are FUTURE (documented, out of v1).
- Mobile-only. No web dashboard in v1. No hardware/POS integration in v1.

## 2. User Summary

Roles (hierarchy: Admin > Manager > Cashier > Washer):

- **Admin**: user management, settings, inventory management, config, day-close reopen.
- **Manager**: open/close day, reconciliation with declared-cash variance, conflict review queue, force-assign/reassign jobs, void approval, create accounts (provisioned via backend).
- **Cashier**: check-in, queue assignment, take cash payment, mark complete, register vehicles, log expenses. CANNOT void paid/claimed jobs, edit prices, apply discounts, see revenue reports, manage inventory/users.
- **Washer**: sees queue + jobs only. No prices, no revenue, no customer contact beyond name/vehicle. Can pull jobs, complete, attach photos, Quality Check (self-check or manager).

Devices: shared counter devices with sign-in/sign-out switching (session persists until sign-out; new sign-in requires network). Per-user audit trails on device.

## 3. Scope Summary

In v1:
- Walk-in + appointment check-in, queue, job lifecycle, cash payment, vehicle/customer registry with history, optional before/after photos, manual inventory + low-stock alerts, expense logging, daily reports + employee performance, local notifications, light-first theme with dark mode, in-app user provisioning, offline-first everything.

Explicitly OUT OF SCOPE (v1):
- Hardware/POS integration, receipt printing, GCash/Maya/cards, push notifications (FCM), customer notifications, web dashboard, multi-branch/tenant, payroll/leave, loyalty program, auto-deduct inventory, free-form appointment times.

## 4. Core Workflows

### Walk-in check-in (3 taps)
1. Plate number → auto-match existing vehicle.
2. Select service preset (Express/Full Detail/Premium) — no price typing.
3. Confirm & Queue.
New vehicle → prompts name + phone (optional). "Last 5 plates" chips on home for one-tap re-entry.

### Appointment booking
Fixed 30-min slots. Offline double-booking resolved by First-Write-Wins: first upload on sync wins the slot; the loser is auto-moved to next available slot and flagged "Rescheduled by system" for Manager.

### Job lifecycle
`Queued → Assigned → In Progress → Quality Check → Completed → Paid` + terminal `Voided`.
- Washers pull next job ("Claim Next") or tap a specific job; Manager/Cashier can force-assign/reassign/reorder.
- First-claim-wins on offline multi-device claims; loser sees "Already claimed".
- Photos: 1–2 before/after, optional, never blocks completion, compressed + deferred upload.

### Payment
Cash at pickup. Payment is its own event (washed ≠ paid). Voids: allowed only while unclaimed AND unpaid; once claimed or paid, void requires Manager approval.

### Day-close (manual)
Manager taps "Close Day" (works offline, idempotent, once per day; reopen = Admin). Auto-generates Daily Report: job count, revenue by payment type, voids with reason, expenses, expected vs declared cash with variance.

### Sync
- Offline queue with retry/backoff; background merge with "synced" indicator.
- Conflict: Last-Write-Wins for routine data; financial events (payments, voids, price edits) go to a Manager conflict-review queue.
- Soft-delete everywhere with tombstones that propagate.
- Server-assigned sequence/timestamp on sync (never device clocks).

### Notifications (v1)
Local only: job-assignment buzz to Washer, day-close summary to Manager. No customer notifications.

## 5. Business Rules

- Cashier void: only if job unclaimed and unpaid.
- Void after claim/payment: Manager approval required.
- Day-close: manual, offline-capable, idempotent, one per day.
- Appointment slot conflict: first-write-wins + auto-reflow to next slot, flagged.
- Job claim conflict: first-claim-wins, loser notified.
- Deletion: soft-delete + tombstone, propagates.
- Financial conflicts: Manager review queue on reconnect.
- Washer never sees prices/revenue/customer contact.
- Sign-in requires network; session persists until sign-out.

## 6. Assumptions

- Single store, single location; no tenant/org modeling in v1 (data model stays extensible for future multi-branch).
- Deployment is a future goal; architecture must stay production-honest (proper auth, Supabase schema built for real deployment).
- Fixed 30-min appointment slots (configurable), not free-form.
- Employee performance reporting included in v1; monthly trends included in V2-T3b.
- Inventory is manual (no auto-deduct); restocking logged as expenses; low-stock thresholds alert.
- Branding: default water-themed palette (blue/teal) used for now, swappable later.
- Receipts are digital/in-app only.
- FCM push notifications deferred to phase 2 (noted as future).
- A new day may be opened while an old one is unclosed; only Admin can retroactively close it.

## 7. Open Questions

- Reporting: employee performance confirmed; monthly trends included in V2-T3b (done).
- Branding: confirm default blue/teal palette is acceptable.
- None blocking architecture.

## 8. Risks

- Offline sync complexity (ordering, clock skew) — mitigated by server-assigned sequences.
- Appointment auto-reflow may surprise customers — requires explicit notice UI.
- Photo sync payload weight — compression + deferred upload so it never starves critical sync.
- Shared devices + offline auth — new sign-in requires network; deactivated users may hold cached sessions (audit + server-side revocation on reconnect).
- Multi-device claims/voids require strict first-write-wins semantics across all mutation types.

## GitHub Workflow (locked)

- Git Flow: `main` (production), `develop`, `feature/*`.
- Conventional commits (`feat:`, `fix:`, ...), short PRs.
- **Phase merge ritual (always do this):** after each phase is verified and approved, merge `develop` into `main` with:
  `git checkout main; git pull origin main; git merge develop; git push origin main` — then switch back: `git checkout develop`.
- Demo strategy: seed script with realistic fake data + one-tap "Demo Mode" pre-populated app + README architecture section.
- Portfolio showcase centerpiece: the sync engine (conflict resolution, offline queue, tombstones).

## Technical Architecture (approved)

### Stack
- Expo (latest stable) + React Native + TypeScript (strict) + Expo Router (file-based routing).
- NativeWind v4 (Tailwind-style) for styling; tokens support light-first + dark mode.
- Zustand (session, UI, ephemeral app state) + TanStack Query (server state, backed by SQLite).
- SQLite via `expo-sqlite` + Drizzle ORM (local-first source of truth).
- Supabase (Postgres mirror + Auth + admin user provisioning). Zod for shape contracts.

### Layered design
1. **Domain** — pure TS entities, business rules, validation (framework-agnostic). Rules live here: void permissions, day-close idempotency, slot reflow, claim-wins.
2. **Data** — Drizzle schema + migrations (local SQLite; remote Postgres mirror keeps column parity), repositories.
3. **Sync engine** (centerpiece) — outbox/change-log on every mutation, retry/backoff queue, server-assigned sequence (never device clocks), LWW resolver, financial-events conflict queue for Manager, deferred low-priority photo upload, tombstone propagation.
4. **State** — Zustand for ephemeral; TanStack Query caches over SQLite-backed repos.
5. **Services** — auth/session, notifications, camera/media, filesystem.
6. **UI** — Expo Router screens + reusable NativeWind components + role-based guards.

### Sync invariants
- Every table: `id` (UUIDv7), `version`, `server_seq`, `deleted_at` (tombstone), `origin_device`.
- Local write → optimistic apply to SQLite → outbox push (ordered) → server assigns `seq` → pull `seq > last_pulled`.
- Routine conflicts: Last-Write-Wins. Financial rows (payments, voids, price edits): routed to `conflict_review` → Manager screen.
- Slots + claims: server-side atomic checks during push (first-write-wins / first-claim-wins).

### Data entities
`users`, `sessions` (local), `roles`, `customers`, `vehicles`, `services` (price presets), `jobs` (status lifecycle, assigned washer), `appointments` (slots, reschedule flags), `payments`, `expenses`, `inventory_items`, `stock_adjustments`, `photos` (before/after, sync priority), `outbox`, `sync_state`, `conflict_review`, `audit_log`, `day_closes`.

### Folder layout (high level)
- `app/` — Expo Router routes (auth, cashier, manager, admin, washer role groups).
- `src/domain/` `src/data/` `src/sync/` `src/services/` `src/stores/` `src/api/` `src/components/` `src/theme/` `src/utils/`
- `drizzle/` — local migrations. `supabase/` — remote schema + migrations + seed. `scripts/` — seed/demo data.

### Auth
Supabase Auth (email/password) + Admin API for in-app provisioning. Offline session persisted until sign-out; new sign-in requires network; deactivated users revoked on reconnect. Per-user audit log on device.

### Notifications (v1)
`expo-notifications` local only: job-assignment buzz (washer), day-close summary (manager). FCM = phase 2.

## Implementation Roadmap (locked, phase-gated)

Each phase ends with a STOP + approval gate (Push to GitHub / Review / UI mockups / Continue).

- **P0 Foundation** — Expo scaffold, TypeScript strict, NativeWind + theme tokens (light/dark), Expo Router skeleton, lint/typecheck, Git Flow init.
- **P1 Local data layer** — Drizzle schema, SQLite migrations, repositories, seed script (vehicles/customers/services).
- **P2 Auth + roles** — Supabase Auth, in-app provisioning, sessions, audit trail, RBAC guards.
- **P3 Check-in flow** — 3-tap walk-in, vehicle match + create, last-5-plates chips, queue screen.
- **P4 Job lifecycle** — statuses, Claim Next, force-assign/reassign, quality check, photos (deferred upload), local notifications.
- **P5 Payments + voids** — cash payment, cashier void rules, Manager approval flow.
- **P6 Appointments** — fixed slots, booking, first-write-wins + auto-reflow, "Rescheduled by system" notice.
- **P7 Inventory + expenses** — manual stock, low-stock alerts, expense logging.
- **P8 Sync engine** — outbox, sequence ordering, conflict resolver + review queue, sync UI, tombstones, photo deferral. (Split into P8a/b/c — largest phase.)
- **P9 Reports + day-close** — daily report, declared-cash variance, employee performance, audit.
- **P10 Polish + demo** — dark mode pass, Demo Mode, seed script, README architecture section, tests, portfolio packaging.

### Phase status

- **P0 Foundation — DONE** (commit `b943fa9`, branch `main`): Expo SDK 57 + TS strict, NativeWind v4.2.6 (Tailwind 3.4, `darkMode: class`) + brand tokens (water palette), Expo Router skeleton with `(auth)` + `(app)` role groups (admin/manager/cashier/washer) + `RoleGuard`, Zustand session store, TanStack Query provider, ESLint (eslint-config-expo flat) + `typecheck`. Git Flow: `main`/`develop` on GitHub.
- **P1 Local data layer — DONE** (on `develop`): Drizzle schema (`src/data/schema/`: customers/vehicles/services + sync columns `id/version/server_seq/deleted_at/origin_device`), migrations generated (`drizzle/`, inlined via `babel-plugin-inline-import` + `metro sourceExts: sql`), DB bootstrap `src/data/db.ts` (gated in root layout), repositories (`src/data/repositories/`), UUIDv7 util, dev seed (`src/data/seed.ts`). `npm run db:generate` regenerates migrations.
- **P2 Auth + roles — DONE** (commit `f31a26d` on `develop`, merged to `main`): Supabase client (`src/api/supabase.ts`, AsyncStorage-persisted session), `src/domain/user.ts` roles (role from `app_metadata.role`, least-privilege fallback `washer`), auth service (`src/services/auth.ts`: sign-in/sign-out/restore, offline-aware errors), Zustand session store (`hydrate/signIn/signOut`, hydrated in root layout), real email/password sign-in screen, `SessionHeader` (role badge + sign out) on all role homes, `audit_log` table (migration `0001`) + repository + `logAudit()` service wired to sign-in/out, in-app provisioning: `provision-user` Edge Function (admin-only, service role) + client `src/services/provisioning.ts`, `supabase/schema.sql` (profiles + signup trigger + RLS). VERIFIED: lint + typecheck + Android bundle export; live sign-in flow works against `slanciuxvgusuperrjdj`. First admin provisioned: `admin@washflow.app` (dev password, change it). PENDING (needs Supabase PAT or dashboard): deploy `provision-user` function + run `schema.sql` (SQL Editor). Supabase CLI v2.111.0 at `%LOCALAPPDATA%\supabase-cli`.
- **P3 Check-in flow — DONE** (commit `5c19509` on `develop`, merged to `main`): `jobs` + `recent_plates` tables (migration `0002`) + repositories, `src/domain/job.ts` status lifecycle, `src/services/checkin.ts` (plate match, check-in, recent plates), TanStack hooks (`src/data/queries.ts`), `formatPesos`/`formatClockTime` utils, cashier screens with bottom tabs (`src/app/cashier/_layout.tsx` via Expo Router `Tabs`, index = check-in, queue) + admin Quick Access links to all role homes. VERIFIED: lint + typecheck + Android bundle export; check-in → queue + tabs work on-device. `@expo/vector-icons` added for tab icons.
- **P4a Job lifecycle — DONE** (commit `6171a83` on `develop`, merged to `main`): transition engine in `src/domain/job.ts` (`JOB_TRANSITIONS`/`canTransition`/`assertTransition`/`WORKING_STATUSES`), `users` local table (migration `0003`, seeded with dev washers, auto-upsert of signed-in users), `JobRepository` atomic `claim`/`assignTo`/`reassign`/`release`/`transition` (first-claim-wins via `WHERE status='queued' AND assigned_to IS NULL`) + washer/working board queries, `src/services/jobs.ts` (claim next, claim, start, mark done, approve QC, force-assign, reassign, release + audit logs), TanStack hooks + mutations, real washer screen (Claim Next, My Jobs with Start/Mark Done/Approve QC, no prices/contact), manager Day Board (grouped by status, Assign/Reassign/Release + washer picker modal). VERIFIED: lint + typecheck + Android bundle export.
- **P4b Photos + local notifications — DONE** (commit `833241b` on `develop`, merged to `main`): `photos` table (`src/data/schema/photos.ts`, kinds before/after, migration `0004_glossy_prodigy.sql` + hand-appended `photos_job_id_idx`) + `PhotoRepository`, `src/services/camera.ts` (expo-image-picker + expo-image-manipulator, resize ≤1280px, JPEG q0.7, stored in `documentDirectory photos/` via `expo-file-system/legacy`), `src/services/photos.ts` (max 2 per kind), `src/services/notifications.ts` (expo-notifications, local only — **gated so the module is never imported inside Expo Go** via `expo-constants` `appOwnership === 'expo'`; Expo Go silently disables notifications since SDK 53 removed Android push there; works in dev/production builds), wiring (configure on boot in root `_layout.tsx`, `notifyAssignment` on force-assign/reassign), washer photo Before/After buttons with counts + camera-or-library picker, `photoKeys`/`useJobPhotos`/`useAddJobPhoto`. VERIFIED: lint + typecheck + Android bundle export; Expo Go boots clean with notifications disabled.
- **P5 Payments + voids — DONE** (commit `6317bd4` on `develop`, merged to `main`): cash-only payment (`src/domain/payment.ts`: `PAYMENT_METHODS = ['cash']` — GCash/Maya/cards future, `payments.method` column extensible), job lifecycle now allows `voided` from any active status (`VOIDABLE_STATUSES`), `payments` + `void_requests` tables (migration `0005_workable_microchip.sql` + hand-appended indexes), `PaymentRepository` + `VoidRequestRepository`, `src/services/payments.ts` (`payJob` atomic completed→paid + cash record, `voidJob` = cashier direct void **only when queued & unpaid**, `requestVoid` = Manager approval flow for claimed/paid, `approveVoidRequest` voids job + reverses payment, `rejectVoidRequest`), `JobRepository.listCompletedWithDetails`, cashier **Collect tab** (completed jobs: Collect ₱ / Request void) + **Void** on queue cards, shared `VoidRequestModal` (reason input), manager **Void Approvals** section (Approve/Reject + requester/reason), seed adds one completed demo job. Every void leaves a `void_request` row (status approved/pending) for day-close reporting. VERIFIED: lint + typecheck + Android bundle export.
- **P6 Appointments — DONE** (commit `14ed7a3` on `develop`, merged to `main`): fixed 30-min slots (configurable via `settings`), `appointments` + `settings` tables (migration `0006_equal_talisman.sql` + hand-appended `appointments_date_idx`/`appointments_status_idx`), `src/domain/appointment.ts` (statuses, `alignToSlot`, date/slot helpers, `formatSlotTime`) + `src/domain/settings.ts` (`SETTING_KEYS`, `DEFAULT_SCHEDULE` 8:00–20:00), `AppointmentRepository` (create/findById/findByIdWithDetails/findBySlot/listForDate/nextAvailableSlot/markCancelled/markCompleted) + `SettingsRepository`, `src/services/appointments.ts` (day slots with past-slot guard, `bookAppointment` first-write-wins at slot OR auto-reflow to next available slot flagged `rescheduled`, `cancelAppointment`, `checkInAppointment` creates a queued job from the booked service + marks appointment completed), `src/services/settings.ts`, TanStack hooks (`useSchedule`/`useDaySlots`/`useBookAppointment`/`useCancelAppointment`/`useCheckInAppointment`), cashier **Bookings tab** (day prev/next, slot grid, `AppointmentBookModal` plate lookup + service picker, amber "RESCHEDULED BY SYSTEM" badge, Check in/Cancel on booked cards), `src/services/checkin.ts` refactored to expose `resolveVehicleCustomer`, seed settings defaults + one booked Premium Detail demo appointment at 9:00. VERIFIED: lint + typecheck + Android bundle export.
- **P7 Inventory + expenses — DONE** (commit `7bfe0d1` on `develop`, merged to `main`): manual stock with low-stock alerts + expense logging. `inventory_items`/`stock_adjustments`/`expenses` tables (migration `0007_soft_speed_demon.sql` + hand-appended `stock_adjustments_item_id_idx`/`expenses_incurred_at_idx`/`expenses_category_idx`), `src/domain/inventory.ts` (`INVENTORY_CATEGORIES`, `ADJUSTMENT_TYPES` restock/usage/waste/correction, `isLowStock`) + `src/domain/expense.ts` (`EXPENSE_CATEGORIES`), `InventoryRepository` (create/update/softDelete/`applyQuantityChange`/`listLowStock`) + `StockAdjustmentRepository` (immutable ledger rows) + `ExpenseRepository` (create/listBetween/softDelete), `src/services/inventory.ts` (`adjustStock` atomic qty change + ledger row + audit; restock/usage signed logic), `src/services/expenses.ts` (`logExpense`/`listDayExpenses`/`dayRange`/`sumExpenses`), TanStack hooks (`useInventory`/`useLowStockItems`/`useStockMovements`/`useAdjustStock`/`useCreateInventoryItem`/`useDayExpenses`/`useLogExpense`), **admin Inventory screen** (`src/app/admin/inventory.tsx`: low-stock amber banner, Add item modal with category/unit/qty/threshold, Adjust stock modal with signed change, Delete) + admin quick link, **cashier Expenses tab** (`src/app/cashier/expenses.tsx`: today's total card + Log expense modal with category chips + day list), seed adds 4 inventory items (one low-stock demo: Glass cleaner 1/2) + one demo expense. VERIFIED: lint + typecheck + Android bundle export.
- **P8a Sync engine core — DONE** (commit `69a7c3e` on `develop`, merged to `main`): outbox change-log on every mutation, ordered flush, server-assigned `server_seq` (never device clocks), pull `seq > last_pulled`, tombstones, sync status UI. `outbox` + `sync_state` tables (migration `0008_secret_doorman.sql` + hand-appended `outbox_entity_idx`/`outbox_status_idx`), `src/data/schema/outbox.ts` (`OUTBOX_OPS` upsert/delete, `OUTBOX_STATUSES` pending/synced/failed, backoff fields) + `src/data/schema/sync-state.ts` (`SYNC_STATE_KEYS.lastPulledSeq`/`lastSyncedAt`), `OutboxRepository` (enqueue/listPending/listDue/listForEntity/countPending/markSynced/markFailed/retryDue) + `SyncStateRepository`, `src/sync/entities.ts` (`SYNC_ENTITIES` registry of 14 entities, `idKey` id except `setting`=key, snake_case `rowToRemote`/`rowFromRemote` column maps; `audit_log` deliberately excluded — local-only), `src/sync/outbox.ts` (`enqueueChange` best-effort), `src/sync/remote.ts` (RPC `sync_upsert`/`sync_changes`), `src/sync/engine.ts` (single-flight `runSync`, `coalescePending` per entity:entityId pushing full-row state, backoff `min(60s, 1000*2^attempts)`, `isDirty` local-wins guard on pull, `getSyncSummary`), `src/sync/hooks.ts` (`useSyncStatus` 10s refetch, `useRunSync`), `src/components/sync-status-bar.tsx` (pending count + last sync + Sync now) mounted in `SessionHeader` on all role homes, boot `runSync()` after session hydrate in root layout, all 14 repositories now call `enqueueChange` after every mutation (create/update/softDelete/status/claim/assign/reassign/release/pay/void). Remote side: `supabase/sync.sql` (server `sync_seq` sequence, `sync_mirror` full-row JSONB store with column parity, `sync_upsert`/`sync_changes` security-definer RPCs with entity allowlist — deploy in SQL Editor, same pending action as P2 `schema.sql`; until deployed the engine treats RPC calls as offline and the outbox grows by design). VERIFIED: lint + typecheck + Android bundle export.
- **P8b Conflict resolution — DONE** (commit `0607a01` on `develop`, merged to `main` via `860adf4`): financial conflicts routed to a Manager review queue; routine rows LWW; server-side first-write-wins for claims + slots with typed conflict responses. `conflict_reviews` table (migration `0009_daffy_kitty_pryde.sql` + hand-appended `conflict_reviews_status_idx`/`conflict_reviews_entity_idx`) + `src/data/schema/conflicts.ts`, `src/domain/conflict.ts` (`CONFLICT_KINDS` payment/void/claim/slot, `FINANCIAL_ENTITIES`, `conflictKindForEntity`, `describeConflict`, resolution/status types), `ConflictReviewRepository` (create/findById/`hasPendingFor`/`countPending`/`listPendingWithDetails` left-joined job+payment/`listRecent`/`markResolved`) — `conflict_reviews` is local-only (not in `SYNC_ENTITIES`) so review rows never re-trigger conflicts, `src/services/conflicts.ts` (`listPendingConflicts`/`countPendingConflicts`/`parseConflictRow`/`resolveConflict` = approved → `applyRemote` + drop queued local change; rejected → leave outbox so local wins by sequence; dismissed → no-op; audit-logged), TanStack hooks (`usePendingConflicts`/`useCountPendingConflicts`/`useResolveConflict`), `src/services/appointments.ts` `reflowAppointmentOnConflict` (auto-move loser slot + `appointment-sync-reflowed` audit), engine `src/sync/engine.ts` (push now consumes `RemotePushResult` — `ok:false` settles the entry instead of retrying, reflows on `slot_taken`, records `claim`/`slot` conflict reviews; pull routes dirty `payment`/`void_request` rows to the review queue instead of LWW; exported `applyRemote`/`clearOutboxFor` resolution helpers), `src/sync/remote.ts` `RemotePushResult` = `{ok:true, serverSeq} | {ok:false, code, message}`, manager **Sync Conflicts** section (amber cards: kind label, description, payment amount, Details modal showing parsed local vs remote rows, Keep remote / Keep local with confirm), `supabase/sync.sql` P8b (advisory-lock serialization + first-claim-wins for `job` assigned + first-write-wins for `appointment` booked slots, `sync_upsert` now returns `{ok, server_seq}` or `{ok:false, code: job_claimed|slot_taken}` — deploy in SQL Editor with P8a/P2). VERIFIED: lint + typecheck + Android bundle export.
- **P9 Reports + day-close — DONE** (commit `db407db` on `develop`, merged to `main` via `a876154`): manual day-close (offline, idempotent, once per day; Admin reopens) + daily report (jobs, revenue, voids, expenses, expected vs declared cash with variance) + per-washer performance. `day_closes` table (migration `0010_wide_umar.sql` + hand-appended `day_closes_day_idx`; `day` unique) + `src/data/schema/day-closes.ts`, `src/domain/day-close.ts` (`DAY_CLOSE_STATUSES`, `DayReport`, `dateKey`/`dayRangeOf`/`formatDay` local-time day helpers, `varianceCents`, `assertValidDeclaredCash`), `DayCloseRepository` (findByDay/create/softDelete=reopen/list; sync entity `day_close` added to `SYNC_ENTITIES` = 15 entities + supabase allowlist), `JobRepository.listFinishedBetween`/`listVoidedBetween` + `PaymentRepository.listBetween`, `src/services/day-close.ts` (`computeDayReport` — revenue excludes voided payments, voids counted separately, `expectedCash = revenue`; `closeDay` actor/declared/notes + audit; `reopenDay` admin + audit; `listDayCloses`/`getDayClose`; `listEmployeePerformance` finished jobs grouped by assigned washer), TanStack hooks (`dayCloseKeys`, `useDayReport`/`useDayClose`/`useDayCloses`/`useCloseDay`/`useReopenDay`/`useEmployeePerformance`), **manager Day Close screen** (`src/app/manager/day-close.tsx`: live "Today so far" card, declared-cash + notes inputs, live variance preview (Balanced/Over/Short), Close day confirm; once closed shows stored report + Team performance list) + Day Board header link with Open/Closed chip, **admin Closed Days screen** (`src/app/admin/day-closes.tsx`: list all closes + Reopen with confirm) + admin Quick Access link. NOTE: P8c photo upload intentionally dropped — photos stay saved on the local device only (user decision). VERIFIED: lint + typecheck + Android bundle export. (Runtime fix `9e8c0e0` merged via main `120d2e4`: `getDayClose` returns `null` not `undefined` — TanStack Query rejects undefined data.)
- **P10 Polish + demo — DONE** (commit `0214110` on `develop`, merged to `main` via `511cad4`): rich demo seed (`src/data/seed.ts` — seeds realistic customers/vehicles/services/washers/inventory/settings/recent plates first, then demo data when the customer table is empty: 5 customers+vehicles, 2 queued / 1 assigned / 1 in_progress / 1 quality_check / 1 completed / 2 paid+payment / 1 voided+approved void_request jobs, 2 booked appointments today (09:00 + 10:30), 2 expenses, 1 closed day (yesterday, variance +200), stock adjustments; seeds are guarded per-entity so re-running is safe), one-tap Demo Mode (`src/services/demo.ts` `loadDemoData` + `useLoadDemoData` hook + "Load demo data" button on sign-in, invalidates all queries), in-app theme toggle (light/dark/system — `src/stores/theme-store.ts` Zustand+AsyncStorage persisted, `setColorScheme(theme)` wired in root `_layout.tsx`, sun/moon/contrast button in `SessionHeader`; screens already carry `dark:` variants), README rewritten with product summary + layered architecture + sync engine + roles + roadmap, Vitest unit tests for pure domain rules (`tests/domain/`: job lifecycle, cashier void policy, day-close dateKey/variance/validation, appointment slots, conflict kinds, inventory low-stock — 29 tests, `npm test`; `vitest.config.mts` with `@` alias → `./src`). NOTE: `useColorScheme`/`setColorScheme` come from `nativewind` (v4 exports them on the hook). VERIFIED: lint + typecheck + `npm test` (29 passing) + Android bundle export. SUPABASE DEPLOY — DONE (see below).
- **Post-P10: Admin user management — DONE** (commit `e5769a4` on `develop`, merged to `main` via `c0b9826`): **admin Manage users screen** (`src/app/admin/users.tsx`: lists local users, Add user modal with name/email/password/role picker, calls `provision-user` Edge Function via `src/services/users.ts` `provisionUserOnServer`, mirrors the account locally via `UserRepository.upsert` + audit-logs) + admin Quick Access link + `useAllUsers`/`useProvisionUser` hooks. VERIFIED: lint + typecheck + Android bundle export.
- **Supabase deploy — DONE** (user + CLI, project `slanciuxvgusuperrjdj`): `schema.sql` + `sync.sql` applied in SQL Editor; `provision-user` Edge Function deployed ACTIVE with `verify_jwt: true` (auto-injects `SUPABASE_SERVICE_ROLE_KEY`; CLI cannot set `SUPABASE_*` names by design — the platform guard skips them). Follow-up SQL fixes (committed/merged to `main`): `schema.sql` made idempotent (`cbaa207`→`ebf26a6`; `do $$ duplicate_object` guard for `app_role` enum, `create table if not exists`, `create or replace function`, `drop trigger/policy if exists`), `sync.sql` got `alter table sync_mirror enable row level security` (`11a0a30`→`6c48d1e`), and `sync_changes` output column renamed from reserved `row` to `payload` (`80439d4`→`9590e47`, `src/sync/remote.ts` reads `change.payload`). `supabase/config.toml` added for CLI deploys (project_id `washflow`, `verify_jwt = true`).

## V2 (post-roadmap, user-chosen scope)

- **V2-T1a Payment methods (cash/gcash/maya/card) — DONE** (commit `eb84f0a` on `develop`): `PAYMENT_METHODS` extended in `src/domain/payment.ts` (`['cash','gcash','maya','card']` + labels + `DEFAULT_PAYMENT_METHOD`), `payJob(jobId, actorId, method)` records method on `payments`, `DayReport.revenueByMethodCents` + `day_closes.revenue_by_method_cents` JSON text column (migration `0012_wakeful_snowbird.sql`), `computeDayReport` builds per-method breakdown, `PaymentMethodModal` (`src/components/payment-method-modal.tsx`) + cashier Collect tab wiring (method badge on history), manager Day Close `MethodBreakdown` (parse + render), `usePayJob` takes `method`, seed uses gcash demo payment + cash/gcash closed-day breakdown, `tests/domain/payment.test.ts` +4. VERIFIED: lint + typecheck + 48 tests + Android bundle export.
- **V2-T1b FCM push notifications (Expo push service) — DONE** (commit `44f67dc` on `develop`): remote push via Expo push service (FCM/APNs routed by Expo tokens). Local `push_tokens` table (migration `0013_bitter_thunderbird.sql` + hand-appended `push_tokens_user_id_idx`; local-only, NOT a sync entity) + `PushTokenRepository` (upsert/find/delete by userId), `src/services/push.ts` (`registerDevicePush` — Expo token via `getExpoPushTokenAsync`, gated on `isExpoGo`, upserts local + remote `push_tokens`; `unregisterDevicePush`; `sendPushToUser` → `send-push` Edge Function; `sendViaExpoPush` direct HTTP helper), remote `push_tokens` table + RLS (own-row insert/update/delete) appended to `supabase/schema.sql`, new `send-push` Edge Function (`supabase/functions/send-push/index.ts`: JWT-verified caller, service-role token lookup, POST to `https://exp.host/--/api/v2/push/send`), `config.toml` + `.example` add `[functions.send-push] verify_jwt = true`, wiring: session store registers token on hydrate/sign-in + unregisters on sign-out, `notifyAssignment` in `src/services/jobs.ts` also sends remote push to the washer. `.env.example` gained `EXPO_PUBLIC_EXPO_PROJECT_ID` (falls back to `app.json extra.eas.projectId`). PENDING (needs Supabase deploy like P2): run the new `push_tokens` block of `schema.sql` in SQL Editor + `supabase functions deploy send-push`; test needs a dev/production build (Expo Go has no push). VERIFIED: lint + typecheck + 48 tests + Android bundle export.
- **V2-T1c Web dashboard (read-only manager/admin) — DONE** (commit `cead4f1` on `develop`): standalone Vite + React + TypeScript + Supabase app in `web/` (own package.json/tsconfig/vite config; Metro does NOT touch it — `web` excluded from root tsconfig, root scripts `web:dev`/`web:build`). Reads the sync mirror through a new security-definer RPC `dashboard_snapshot(p_after_seq)` in `supabase/dashboard.sql` (returns all non-tombstoned mirror rows newest-first; **manager/admin only** via `is_dashboard_reader()` role gate, grant to `authenticated`). `web/src/api.ts` fetches + splits the snapshot into typed collections; views: **Today** (queue grouped by status, completed list, today's bookings), **Revenue** (today's payments by method + transaction list with cashier), **Day close** (latest close with variance badge + method breakdown via `parseMethodBreakdown`, history list), **Inventory** (low-stock + on hand). Auth: Supabase email/password sign-in, role chip + sign-out in topbar, light-first + dark-mode CSS with the water palette. Env: `web/.env.example` (`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`). Follow-up fix `73739f3`: renamed `dashboard_snapshot` output column `row` → `payload` (reserved word, same as `sync_changes`). Post-commit UI add (theme toggle, uncommitted until T2a commit): `web/src/theme.ts` hook + `ThemeToggle` in topbar/sign-in card, localStorage `washflow-theme`, `data-theme` attr + pre-paint script, CSS `html[data-theme='dark']` selectors. PENDING (needs Supabase deploy): run `supabase/dashboard.sql` in SQL Editor. VERIFIED: `npm run web:build` (tsc + vite) + mobile lint/typecheck/48 tests + Android bundle export unaffected.
- **V2-T2a Digital receipts — DONE** (commit `0f49b36` on `develop`): in-app + shareable receipts for settled payments. `src/domain/receipt.ts` (pure: `Receipt` model, `buildReceiptNumber` `WF-YYYYMMDD-<last5 payment id>`, `formatPesosForReceipt` with thousands separators, `formatReceiptIssuedAt`, `receiptLineItems`, `receiptText` for sharing), `PaymentRepository.findById` added, `src/services/receipts.ts` (`buildReceiptForPayment`/`buildReceiptForJob` — loads payment + job/vehicle/customer/service details + collector name from local users), TanStack hooks (`useReceiptForPayment`/`useReceiptForJob`, `paymentKeys.receipt`), `src/components/receipt-modal.tsx` (stylized dashed receipt card, paid badge, Share via React Native `Share`), cashier Collect screen wiring: after `payJob` succeeds the receipt opens automatically, and a "Receipt" link on every history entry re-opens it. `tests/domain/receipt.test.ts` +7. VERIFIED: lint + typecheck + 55 tests + Android bundle export.
- **V2-T2b Free-form appointment times — DONE** (commit `2f4da24` on `develop`): appointments can now be booked at arbitrary times/durations, not just fixed 30-min slots. `src/domain/appointment.ts` gains `appointmentWindowsOverlap` (pure overlap check), `buildStartTimestamp`, `DEFAULT_APPOINTMENT_DURATION` (30) + `APPOINTMENT_DURATION_OPTIONS` [15,30,45,60,90,120]; `AppointmentRepository.findConflicting` (overlap-based SQL window check using `duration_minutes`) + `findNextFreeStart` (walks forward by `slotMinutes` until a non-overlapping window fits before close); `src/services/appointments.ts` `bookAppointment` now takes `durationMinutes` and overlap-conflicts → reflow via `findNextFreeStart` (requested time kept as `rescheduledFrom`), new `findAppointmentConflict` service + `useAppointmentConflict` hook (live amber "overlaps another booking" warning); `AppointmentBookModal` gained `freeForm` mode (start-time ±5 min stepper + duration chips + conflict notice, remounted via `key` per target); cashier Bookings screen has a **Custom time** button opening free-form booking. Server `supabase/sync.sql` `sync_upsert` appointment check changed from exact `slot_start` equality to window-overlap (`slot_taken` on overlap, needs re-deploy in SQL Editor). `tests/domain/appointment.test.ts` +5. VERIFIED: lint + typecheck + 60 tests + Android bundle export.
- **V2-T2c Customer notifications (local alert + share-link status) — DONE** (commit `09de191` on `develop`): customer notices that never leave the device — a local counter alert plus a shareable status text the cashier can send the customer via SMS/messenger. `src/domain/customer-notice.ts` (pure: `CUSTOMER_NOTICE_MILESTONES` queued/ready/paid, `buildCustomerNotice` builds brand-aware share text with optional service name), `src/services/customer-notices.ts` (`buildNoticeForJob` loads job/vehicle/customer/service and returns the message, `notifyReadyForPickup` fires a local "Vehicle ready for pickup" notification), wiring: `approveQuality` in `src/services/jobs.ts` fires `notifyReadyForPickup` on completion, cashier Collect screen shows a **Notify** button on ready-to-collect cards that opens the native share sheet with the ready message. `tests/domain/customer-notice.test.ts` +7. VERIFIED: lint + typecheck + 67 tests + Android bundle export.
- **V2-T3a Auto-deduct inventory — DONE** (commit `3026f37` on `develop`): services can declare inventory recipes and completing a job auto-deducts them. `service_inventory_items` table (migration `0014_green_gravity.sql` + hand-appended `service_inventory_service_id_idx`/`service_inventory_item_id_idx`) + `src/data/schema/service-inventory.ts`, sync entity `service_inventory_item` added to `SYNC_ENTITIES` (now 16) + supabase allowlist, `src/domain/service-inventory.ts` (pure: `assertValidQuantityUsed`, `computeAutoDeduction` clamps to on-hand so stock never goes negative), `ServiceInventoryRepository` (`listAll`/`listForService`/`listAllWithDetails` joined + `replaceForService` soft-delete+recreate), `src/services/service-inventory.ts` (`listServiceUsageConfig`/`listUsagesForService`/`saveServiceUsages` admin recipe save with duplicate-item guard/`autoDeductForJob` writes `usage` adjustments via `adjustStock` reason `Auto-deduct · <service>`, never blocks completion), wiring: `approveQuality` in `src/services/jobs.ts` calls `autoDeductForJob` (non-fatal try/catch), TanStack hooks (`useServiceUsageConfig`/`useSaveServiceUsages` + `serviceInventoryKeys`), **admin Service recipes screen** (`src/app/admin/service-inventory.tsx`: per-service recipe cards + edit modal with 0/1/2/3/5 quick amounts per item) + admin Quick Access link, seed maps demo recipes (Express 1×shampoo, Full Detail 1×shampoo+2×towels, Premium 1×shampoo+3×towels+1×protectant). PENDING (needs Supabase deploy): re-run `supabase/sync.sql` allowlist change in SQL Editor. `tests/domain/service-inventory.test.ts` +5. VERIFIED: lint + typecheck + 72 tests + Android bundle export.
- **V2-T3b Monthly trends — DONE** (commit `ae2e13f` on `develop`): month-level reporting in the Manager app. `src/domain/monthly.ts` (pure: `monthKey`/`monthRangeOf` local-time inclusive range/`formatMonth`/`shiftMonth`/`isValidMonthKey`/`averageRevenuePerDay`, `MonthReport` model), `src/services/monthly.ts` (`computeMonthlyReport` aggregates `payments.listBetween` revenue by method with voided payments excluded + `jobs.listFinishedBetween` count + `jobs.listVoidedBetween` count/amount + `expenses.listBetween` + `day_closes` closed-day count, `activeDayCount` distinct local days with any payment/finished job/expense, `netCents = revenue - expenses`, `avgRevenuePerDayCents`; `listMonthlyEmployeePerformance` per-washer finished count + revenue for the month), TanStack hooks (`monthlyKeys` + `useMonthlyReport`/`useMonthlyEmployeePerformance`), **manager Monthly trends screen** (`src/app/manager/trends.tsx`: prev/next month picker, Summary card with revenue + method breakdown + voids + expenses + net, Activity card with active days + avg revenue/day + days closed, Team performance list) + Day Board link. `tests/domain/monthly.test.ts` +7. VERIFIED: lint + typecheck + 79 tests + Android bundle export.
- **V2-P1 Polish + demo pass — DONE** (commit `d856dbb` on `develop`): app-wide polish + demo improvements. **RBAC security fix**: cashier tab group (`src/app/cashier/_layout.tsx`) now wrapped in `RoleGuard roles={['cashier','manager','admin']}` — previously any authenticated user (incl. washer) could deep-link to `/cashier/collect` and see prices/payments. **README overhaul**: added V2 section, moved shipped V2 features out of "Out of scope", updated Notifications (FCM shipped), Deployment (adds `send-push` + `dashboard.sql`), fixed quick-start code block, folder layout (`web/` + seed lives in `src/data/seed.ts`). **Dead code removed**: `src/components/placeholder-screen.tsx`, `src/theme/index.ts` barrel (nothing imported `@/theme`), `resetDeviceId` (`src/services/device.ts`), unused hooks `useCountPendingConflicts`/`useSchedule`/`useStockMovements`/`useUpdateInventoryItem` + orphaned services `countPendingConflicts`/`listStockMovements`/`updateInventoryItem` + unused key consts (`conflictKeys.pendingCount`, `scheduleKeys`, `inventoryKeys.movements`). **Dark-mode fixes**: manager "Sync conflicts" header `text-amber-600 dark:text-amber-400`, session-header theme icon now `brand[400]`/`brand[700]` via `useColorScheme` (was hardcoded `#0E7490`). **Demo**: seed adds a *pending* void request (queued job, "Customer found a scratch...") so the Manager Void approvals section shows in demo mode; cashier Bookings screen gained `RefreshControl` pull-to-refresh (was the only list screen without one). **Dedup**: extracted shared `StatRow`/`MethodBreakdown`/`parseMethodBreakdown` into `src/components/report-rows.tsx` (was duplicated in `manager/day-close.tsx` + `manager/trends.tsx`). VERIFIED: lint + typecheck + 79 tests + Android bundle export.
